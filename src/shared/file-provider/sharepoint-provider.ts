/**
 * SharePoint File Provider — PLACEHOLDER
 *
 * This file will eventually contain the SharePoint implementation
 * of the FileProvider interface. It is not implemented in Stage 1.
 *
 * When implemented (Stage 3), this will:
 * - Connect to SharePoint via Microsoft Graph API
 * - Upload files to a configured SharePoint document library
 * - Download files from SharePoint
 * - List files associated with a record
 * - Delete files from SharePoint
 *
 * Because this is behind the FileProvider interface, SharePoint
 * can be replaced with any other storage backend (S3, Azure Blob,
 * local disk) by swapping this file for a different implementation.
 */

// import { FileProvider, FileMetadata, UploadResult } from "./types";
//
// export class SharePointProvider implements FileProvider {
//   async upload(...) { ... }
//   async download(...) { ... }
//   async listByRecord(...) { ... }
//   async delete(...) { ... }
// }

export {};
